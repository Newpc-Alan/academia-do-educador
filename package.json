{
  "name": "academia-do-educador",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/domain.test.js tests/interface.test.js",
    "check": "node scripts/verificar.mjs",
    "test:emulator": "firebase emulators:exec --project demo-academia --only firestore,storage \"node --test --test-concurrency=1 tests/integration.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.4",
    "firebase": "10.12.2",
    "firebase-tools": "13.35.1",
    "jsdom": "^26.1.0"
  }
}
